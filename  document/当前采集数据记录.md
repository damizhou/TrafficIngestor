# 当前采集数据记录

统计时间：2026-06-01 09:39 +08:00

## 统计范围

本文记录当前工作区 `E:\Study\Code\TrafficIngestor` 内可见的数据文件和采集产物。仓库中多个采集入口的 `BASE_DST` 指向 `/netdisk`、`/netdisk2` 等外部挂载路径；这些外部目录未在本次统计中扫描，因此不把它们计入“已核实”的本地采集结果。

采集器设计上的标准输出包括 `pcap`、`ssl_key`、`html`、`screenshot` 和页面正文 `content/text`。当前工作区内实际可见的已采集产物主要是 `temp/zh.wikipedia.org` 下的 `pcap` 文件；未在工作区内发现同批次的 `ssl_key`、`html`、`screenshot` 或 `content/text` 产物。

## 本地可见采集产物

本地工作区当前可核实的抓包产物覆盖 1 个域名、2 个有产物的采集轮次，共 110 个 `pcap` 文件，总大小 4,162,590,746 bytes，约 3.88 GiB。

| 域名 | 采集轮次目录 | PCAP 数量 | 覆盖 URL ID | 大小 | 说明 |
| --- | --- | ---: | --- | ---: | --- |
| `zh.wikipedia.org` | `temp/zh.wikipedia.org/20260528092601` | 100 | `149, 869, 2017, 3351, 5344, 5381, 5960, 6248, 6505, 6852` | 3,962.00 MiB | 每个 URL ID 约 10 个重复抓包；文件名时间范围约为 `20260528_15_00_50` 至 `20260528_15_02_36`。 |
| `zh.wikipedia.org` | `temp/zh.wikipedia.org/20260529092553` | 10 | `149, 869, 2017, 3351, 5344, 5381, 5960, 6248, 6505, 6852` | 7.76 MiB | 每个 URL ID 1 个抓包；文件名时间范围约为 `20260529_09_22_25` 至 `20260529_09_22_34`。 |
| `zh.wikipedia.org` | `temp/zh.wikipedia.org/20260529094956` | 0 | 无 | 0 MiB | 目录存在，但未产生 `pcap` 文件。 |

本地产物命名遵循 `{url_id}_{YYYYMMDD_HH_MM_SS}_{domain}.pcap`，例如 `149_20260529_09_22_25_zh.wikipedia.org.pcap`。这一命名可以直接用于按 URL ID、时间戳和域名回溯采集事件。

## URL 与任务输入数据

`small_tools/origin` 保存原始 URL 或排名来源。当前可见文件如下：

| 文件 | 规模 | 说明 |
| --- | ---: | --- |
| `small_tools/origin/tranco_ZWZ5G.csv` | 5,497,921 行 | Tranco 排名来源，无 `id,url,domain` 表头，行内容形如 `rank,domain`。 |
| `small_tools/origin/x_url.txt` | 2,657 行 | X/Twitter URL 原始列表。 |
| `small_tools/origin/bsky_urls_1000.txt` | 1,000 行 | Bluesky URL 原始列表。 |
| `small_tools/origin/mastodon_social_urls_1000.txt` | 1,000 行 | Mastodon URL 原始列表。 |
| `small_tools/origin/threads_urls_1000.txt` | 1,000 行 | Threads URL 原始列表。 |
| `small_tools/origin/homeonly_merged_origin.csv` | 2,071 行 | 主页数据原始合并文件，包含表头。 |

`small_tools/result` 保存已整理为采集任务格式的 CSV。除 Tranco 排名文件外，主要任务 CSV 均使用 `id,url,domain` 表头。

| 文件或文件组 | 任务行数 | 用途 |
| --- | ---: | --- |
| `tranco_ZWZ5G.csv` | 5,497,921 行 | Tranco 排名副本，仍为 `rank,domain` 格式。 |
| `wiki_white.csv` | 300,000 | 维基白名单任务集合。 |
| `top300000_ingestor.csv`, `top300000_ingestor_temp.csv` | 254,424 / 文件 | Top 站点采集任务集合。 |
| `top200000_ingestor_subpage.csv` | 200,001 | Top 站点子页面采集任务集合。 |
| `x_url_list.csv` | 132,850 | X/Twitter 采集任务集合。 |
| `wiki.csv`, `wiki_chrome.csv`, `wiki_edge.csv`, `wiki_firefox.csv` | 87,840 / 文件 | 维基采集任务及浏览器专用副本。 |
| `homeonly_merged.csv`, `homeonly_merged_chrome.csv`, `homeonly_merged_edge.csv`, `homeonly_merged_firefox.csv`, `homeonly_merged_chrome_eu.csv`, `homeonly_merged_chrome_sgp.csv`, `homeonly_merged_chrome_trojan.csv` | 82,800 / 文件 | 主页采集任务及不同浏览器、地区或 Trojan 切片副本。 |
| `subpages_merged.csv` | 82,800 | 子页面采集任务集合。 |
| `bsky_urls_1000.csv`, `mastodon_social_urls_1000.csv`, `threads_urls_1000.csv` | 50,000 / 文件 | 社交平台采集任务集合，来自各自 1,000 URL 原始列表的扩展版本。 |
| `wiki10.csv` | 21,960 | 维基小规模重复采集任务集合。 |
| `homeonly_merged_10.csv` | 20,700 | 主页小规模重复采集任务集合。 |
| `test10.csv`, `test10_fra.csv` | 100 / 文件 | 10 URL 级别的测试或地区采集任务。 |
| `test.csv`, `test1.csv` | 10 / 文件 | 单轮小样本测试任务。 |
| `wiki_temp.csv` | 4 | 临时维基任务。 |

## 已配置的主要采集方向

从当前入口脚本可以看出，仓库已配置的采集方向包括：

- 维基页面：`wiki_chrome.csv`、`wiki_edge.csv`、`wiki_firefox.csv` 分别对应 Chrome、Edge、Firefox 普通或 Trojan/Clash 采集入口。
- Top 站点主页与子页面：`homeonly_merged*.csv` 和 `subpages_merged.csv` 用于主页、子页面以及不同地区或代理切片。
- 社交平台页面：`x_url_list.csv`、`bsky_urls_1000.csv`、`mastodon_social_urls_1000.csv`、`threads_urls_1000.csv` 对应 X、Bluesky、Mastodon、Threads。
- 小规模验证任务：`test.csv`、`test1.csv`、`test10.csv`、`test10_fra.csv`、`wiki_temp.csv` 用于复采、浏览器替换或地区链路验证。

## 当前完整性判断

当前工作区可直接核实的已采集流量数据只有 `zh.wikipedia.org` 的本地 `pcap` 临时产物。生产级结果很可能已经被移动到各入口脚本配置的 `/netdisk` 或 `/netdisk2` 输出目录；如果需要形成完整数据集台账，需要在采集主机上挂载并扫描这些外部结果目录。

部分历史 CSV 在 PowerShell 终端中显示出中文 URL 编码乱码，但行数、文件大小和 `id,url,domain` 结构仍可统计。后续如要发布数据说明，建议单独检查这些文件的编码和 URL 可读性。
